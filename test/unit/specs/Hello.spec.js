import Vue from 'vue'
import Calc from '@/components/Calc'

describe('Calc.vue', () => {

  it.skip('should concatenate display number', function(done){

  })

  /**
   * @todo When operand button is clicked, the current number will show in
   * smaller font above display. All operand btns are disabled except the cancel
   * and equals. The next number can now be typed.
   */
   it.skip('should cache the number on operand btn click', function(){

   })

  it.skip('should run button operand', function(done){

  })



  // it('should render correct contents', () => {
  //   const Constructor = Vue.extend(Calc)
  //   const instance = new Constructor().$mount()
  //
  //   expect(instance.$el.querySelector('.hello h1').textContent)
  //     .to.equal('Welcome to Your Vue.js PWA')
  // })
})
